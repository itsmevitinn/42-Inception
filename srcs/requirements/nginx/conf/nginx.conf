daemon off;

events {}

http {
	server {
		listen      443 ssl;
    	server_name vsergio.42.rio;
    	root    /var/www/html;
    	index index.php;
		ssl_certificate /etc/nginx/nginx-selfsigned.crt;
		ssl_certificate_key /etc/nginx/nginx-selfsigned.key;
		ssl_protocols TLSv1.3;
	}
}